setup:
  addons:
    - plan: heroku-postgresql
build:
  docker:
    web: Dockerfile
run:
  web: ./manage.py runserver 0.0.0.0:$PORT
release:
  image: web
  command:
    - python manage.py migrate